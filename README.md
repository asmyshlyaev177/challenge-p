# challenge-p

## How to use

### Prerequisites

  Recommended to use `nvm` or `node` 20+

### Install

  Run `npm install` in a root directory

### Run

Run `npm run app input.json`

### Tests

Run `npm run test`
